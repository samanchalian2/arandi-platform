import { AdminEmptyState } from "./AdminEmptyState";

export function AdminCardEmptyState() {
    return (
        <AdminEmptyState
            title="No Cards in this Section"
            description="Cards created for this Section will appear here in canonical order."
        />
    );
}
