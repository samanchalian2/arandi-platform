(()=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const scenes=[...document.querySelectorAll('.scrollwise-scene')];const observer=new IntersectionObserver(entries=>entries.forEach(entry=>entry.target.classList.toggle('is-active',entry.isIntersecting)),{threshold:.45});scenes.forEach(scene=>observer.observe(scene));addEventListener('scroll',()=>scenes.forEach((scene,index)=>{const rect=scene.getBoundingClientRect(),progress=Math.max(0,Math.min(1,(innerHeight-rect.top)/(innerHeight+rect.height)));scene.style.setProperty('--camera-x',`${8+progress*84}%`);scene.style.setProperty('--camera-scale',1.015+progress*.075)}),{passive:true});dispatchEvent(new Event('scroll'));})();
