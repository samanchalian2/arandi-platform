# Arandi WordPress Edition

This folder contains the Git-tracked WordPress application layer only: two GeneratePress child themes, the Arandi Core plugin, and deployment scripts. WordPress core, uploads, caches, database dumps, and secrets are excluded.

WordPress uses its own MariaDB database and never reads or writes the Next.js PostgreSQL database.
